from setuptools import setup, find_packages

setup(
    name='hranilishe',
    version='0.0.1',
    packages=find_packages(),
    description='всякие полезняхи при работе с хранилищами медиа данных',
    install_requires=[
        'Django>=1.8'
    ]
)
